# 超声语音匹配引擎 — 设计文档（完整版）

> 版本: v5.3 | 更新: 2026-06-07
> 定位: 医生语音→实时匹配→自动锁定→生成完整报告

---

## 一、引擎架构

```
医生说 → [语音ASR] → [方言标准化] → [器官检测] → [模板匹配] → [置信度评分] → [≥85%自动锁定] → [生成完整报告]
                               ↓                    ↓             ↓                  ↓
                       方言映射表(20+)      183模板按器官分组    连续评分函数       随访建议/同义库
```

### 核心流程

1. **语音输入** — 浏览器 Web Speech API / 阿里云百炼 ASR
2. **方言标准化** — 川渝方言→标准术语 + 中文数字→阿拉伯数字
3. **器官检测** — 根据关键词锁定检查部位（肝脏/甲状腺/前列腺/乳腺/颈动脉/心脏/双肾/胆囊等12类）
4. **模板匹配** — 按器官缩小候选集，多源文本匹配（tpl_see + hint + sample + full + discname）
5. **置信度评分** — 连续函数：短语命中 + 字符Jaccard + 句子包含率 + 长短语匹配
6. **自动锁定** — 置信度≥85%自动锁定，生成完整报告+诊断提示+随访建议
7. **自学习** — 医生纠正→系统自动记忆新映射

---

## 二、数据源

| 数据 | 来源 | 数量 | 用途 |
|------|------|------|------|
| 模板库 | `ultrasound_report_templates.json` | 183个模板 | 匹配索引 |
| 疾病分组 | 模板库内嵌 | 22组 | 器官过滤 |
| 诊断同义库 | `diagnosis_synonyms.json` | 183个rid别名 | 增强匹配 |
| 随访规则 | `followup_rules.json` | 44条 | 锁定后生成建议 |
| 数值阶梯 | `size_thresholds.json` | 63个尺寸规则 | 数值合理性校验 |
| 回声指纹 | `echo_fingerprint.json` | 54个疾病 | 回声匹配校验 |
| 方言映射 | `dialect_recognition_rules.json` | 20+条 | 方言标准化 |
| 共现推荐 | `cooccurrence.json` | 108个rid | 多病灶识别 |

---

## 三、器官检测规则（12类）

```
肝脏   → 含"肝脏/肝内/脂肪肝/肝囊肿/肝硬化"等14个关键词
甲状腺 → 含"甲状腺"
前列腺 → 含"前列腺/膀胱充盈"
乳腺   → 含"乳腺/双乳/腋窝"
颈动脉 → 含"颈动脉/颈总/斑块/内中膜"等10个关键词
心脏   → 含"心脏/AO/LA/二尖瓣/房室间隔/心功能"等14个关键词
双肾   → 含"肾/肾上腺/肾动脉"
子宫附件→ 含"子宫/卵巢/附件/宫腔/内膜"
胆囊   → 含"胆囊/胆总"
腹部   → 含"门静脉/脾/胰腺"
下肢血管→ 含"下肢/股总/腘/胫前"
阴囊   → 含"睾丸/附睾/精索"
```

**匹配流程**：先检测用户语音中出现的器官 → 只在该器官组的模板中搜索 → 检测不到则全量搜索

---

## 四、置信度评分函数

### 4.1 输入
- 用户语音文本 `T`
- 模板条目 `M`（含 `match_pool` = 多源拼接文本）

### 4.2 评分维度

| 维度 | 权重 | 计算方法 |
|------|------|---------|
| 短语命中 | 25% | 22个高频短语的加权累加（每个 6-15分） |
| 字符Jaccard | 20% | clean(T) 与 clean(match_pool) 的字符集相似度 |
| 句子包含率 | 15% | 用户说的短句中有多少完整出现在模板中 |
| 长短语匹配 | 15% | 19个长短语完全命中的累加 |
| discname精确 | 10% | 模板名称完整命中（如"脂肪肝"在输入中出现） |
| 组合加成 | 15% | 多个维度同时高时额外加分 |

### 4.3 评分示例

```
用户说："肝脏形态规则，大小正常，表面光滑，实质回声分布均匀，肝内管系尚清"
命中模板：肝脏脂肪沉积(xy) → 置信度 98%

得分明细:
  短语命中: "未见明显"0 + "大小正常"8 + "表面光滑"8 + "实质回声"6 + "肝内管系"10 = 32 → 25%
  字符Jaccard: 72% → 18%
  句子包含率: 5句全包含 = 100% → 15%  
  长短语: "肝内管系尚清""实质回声分布均匀" = 2 → 10%
  discname: "肝脏脂肪沉积"不完全命中 → 0
  组合加成: 句子≥3 + Jaccard≥0.5 → 8%
  → total = 98%
```

### 4.4 锁定阈值

- **≥85%**: 自动锁定，生成完整报告
- **70-84%**: 显示 TOP1-3 候选项，等待医生确认
- **<70%**: 显示候选项，建议医生补充更多描述

---

## 五、方言标准化

### 5.1 方言→标准映射

| 方言 | 标准 | 领域 |
|------|------|------|
| 腰子 | 肾 | 泌尿 |
| 水泡泡 | 囊肿 | 通用 |
| 石头 | 结石 | 胆囊/泌尿 |
| 头头 | 颈部 | 器官顶端 |
| 包包 | 结节 | 乳腺/体表 |
| 回音 | 回声 | 通用 |
| 没得 | 无明显 | 通用否定 |
| 阴了 | 阴性 | 通用 |
| 花花 | 点状强回声 | 甲状腺/乳腺 |

### 5.2 中文数字→阿拉伯数字

```
"点八" → ".8"
"一点二" → "1.2"  
"零点五" → "0.5"
```

---

## 六、生成完整报告

### 6.1 输出字段

```json
{
  "rid": 408,
  "discname": "肝脏脂肪沉积(xy)",
  "discgroup": "肝脏",
  "tpl_see": "肝脏形态规则，大小正常，表面光滑...",
  "tpl_hint": "肝脏脂肪沉积。",
  "full_report": "肝脏形态规则，大小正常...",
  "suggestion": "定期复查（1年）",
  "confidence": 0.98
}
```

### 6.2 随访建议

按 rid 匹配 `followup_rules.json` 中的规则：
- TI-RADS 2类 → 定期复查1年
- TI-RADS 4a类 → 建议穿刺活检
- BI-RADS 2类 → 定期复查1年
- 脂肪肝 → 建议消化内科就诊，注意饮食

---

## 七、已知限制

| 限制 | 原因 | 影响范围 |
|------|------|---------|
| 模板缺字 | `tpl_see` 开头缺"甲"/"肝"等字 | 10个模板(5%) |
| 占位符干扰 | 模板含 `xmm`/`左/右` 等 | 97个模板(53%) |
| 单一匹配 | 不支持多病灶复杂报告 | 约30%真实报告 |
| 纯文本输入 | 未接入真实ASR | 依赖浏览器语音 |
| 评分天花板 | 连续评分有上限 | 高分段区分度不够 |

---

## 八、后续增强建议（按优先级）

### P0: 修复模板数据
- 补全 `tpl_see` 开头缺失的器官字
- 用真实报告文本重建匹配索引（替换含占位符的模板文本）

### P1: 接入数值阶梯+回声指纹
- 医生说"50x30mm"但肝囊肿典型范围6~40mm → 降置信度
- 医生说"强回声"但模板回声指纹是"无回声86%" → 降置信度

### P2: 多病灶支持
- 合并 `voice_nodule_engine.py`，一条报告中4个结节各自分级+分别匹配

### P3: 真语音ASR接入
- 浏览器 Web Speech API / 阿里云百炼 ASR SDK → 输出直接喂给 match()

### P4: 自学习闭环
- 医生修正→系统自动记忆→下次同样输入正确命中
