# 随行商旅 — 交互设计深度解析

## 写给设计师和产品经理的交互思想教程

---

## 目录

1. [交互设计的核心哲学](#一交互设计的核心哲学)
2. [交互架构：8页面的关系拓扑](#二交互架构8页面的关系拓扑)
3. [逐页交互深度分析](#三逐页交互深度分析)
4. [10种交互模式库](#四十种交互模式库)
5. [微交互规范：节奏决定品质感](#五微交互规范节奏决定品质感)
6. [认知负荷管理：为什么这样设计](#六认知负荷管理为什么这样设计)
7. [交互思考框架：如何系统做交互设计](#七交互思考框架如何系统做交互设计)

---

## 一、交互设计的核心哲学

### 1.1 什么是"交互深度"？

交互深度不是"页面层级多"，而是**用户能在多短的时间内、以多小的认知负担、完成多有价值的任务**。衡量标准：

| 浅交互（差） | 深交互（好） |
|------------|-----------|
| 页面很多，但每页信息很少 | 页面很少，但每页在关键时刻给出关键信息 |
| 用户需要自己找功能 | 功能在用户需要时自动出现 |
| 每次操作需要看屏幕 | 反馈多通道（视觉+触觉+语音） |
| 操作后不知道发生了什么 | 每次操作有清晰的即时反馈 |
| 所有用户看到的都一样 | 不同场景/角色看到不同的内容 |

### 1.2 随行商旅的7条交互铁律

**铁律1：策展优先（Curate over Search）**

```
传统模式：用户输入 → 系统返回列表 → 用户筛选 → 用户决定
策展模式：系统理解意图 → 给出1-2个最优方案+理由 → 用户确认

交互意义：每减少一个"选择"步骤，转化率提升15-30%（基于Hick's Law）
```

**铁律2：解释每一笔（No Mystery Recommendations）**

```
错误方式：推荐"徽商故里 · ★4.8 · ¥280"
正确方式：推荐"徽商故里" + "为什么？因为距客户步行8分钟，包间私密，徽菜正宗不出错"

交互意义：没有解释的AI推荐 = 用户不信任 → 不使用。解释是建立AI信任的唯一方式。
```

**铁律3：渐进式披露（Progressive Disclosure）**

```
L1 首页：只看到"有一条待出行计划"
L2 方案页：看到完整的1个最优方案
L3 详情/对比：看到备选方案和深度信息
L4 出行中：看到时间线和动态通知

交互意义：每层信息密度递增，但用户始终只面对"当前这一步"需要的决策。
```

**铁律4：安静地守护（Quiet Competence）**

```
错误方式：推送"您的航班将在2小时后起飞，请尽快出发"
正确方式：用户正在开会 → 手表轻震 → 通知栏静默显示 → 用户方便时查看

交互意义：商务场景中，打扰本身是一种失礼。好的交互是"用户需要时我在，不需要时我消失"。
```

**铁律5：拇指热区优先（Thumb Zone First）**

```
┌──────────────────┐
│  ← 导航区（点击少）│
│                  │
│   内容区（可滚动） │
│                  │
│  ← 主要操作区     │  ← 拇指自然触达区域
│  [确认] [取消]   │     最重要的按钮必须在这里
└──────────────────┘

交互意义：主要CTA（Call to Action）永远在屏幕下半区，用户不需要调整握姿。
```

**铁律6：得体的默认值（Sensible Defaults）**

```
错误方式：所有选项都让用户选（"您要一等座还是二等座？"）
正确方式：默认选一等座（因为商务出行），用户可以改，但大多数情况不需要改

交互意义：默认值是设计师替用户做的功课。好的默认值 = 用户省去80%的操作。
```

**铁律7：记忆比操作重要（Memory over Manipulation）**

```
传统App价值：操作效率（几步能订完票）
随行商旅价值：记忆效率（下次见王总时自动想起他胃不好）

交互意义：第一次使用时用户花30秒录入客户信息，换来此后每一次的自动匹配。
这是"前期小投入→长期零操作"的投资型交互模型。
```

---

## 二、交互架构：8页面的关系拓扑

### 2.1 页面拓扑图

```
                    ┌─────────────┐
                    │  Screen 1   │
                    │  首页 Home   │ ← 所有旅程的起点
                    └──┬───┬───┬──┘
                       │   │   │
          ┌────────────┘   │   └────────────┐
          ▼                ▼                ▼
    ┌───────────┐   ┌───────────┐    ┌───────────┐
    │ Screen 2  │   │ Screen 6  │    │ Screen 7  │
    │ 方案结果   │   │ 出行中     │    │ 品味画像   │
    └──┬───┬────┘   └───────────┘    └───────────┘
       │   │
       ▼   ▼
  ┌──────────┐  ┌───────────┐
  │ Screen 3 │  │ Screen 4  │
  │ 方案对比  │  │ 客户档案   │
  └──────────┘  └──┬────────┘
                   │
                   ▼
            ┌───────────┐
            │ Screen 8  │
            │ 接待策略   │
            └───────────┘

  Screen 5（商务须知）：可从Screen 2/6中的城市标签跳转
```

### 2.2 导航模型

| 导航方式 | 使用场景 | 动画 |
|---------|---------|------|
| **Push推入** | 首页→方案 方案→对比 方案→客户档案  客户→策略 | 从右滑入（暗示"前进"） |
| **Pop弹出** | 任何页面点返回 | 向右滑出（暗示"回去"） |
| **Tab切换** | 首页/行程列表/客户列表/我的 | 即时切换（暗示"平级"） |
| **Modal弹出** | 确认预订、录入客户 | 从底部升出（暗示"临时操作"） |
| **Deep Link** | 推送通知→出行中页面 | 直接出现（暗示"紧急"） |

### 2.3 页面层级深度

```
L0（系统层）：通知/Widget/手表——用户不打开App就能获取信息
L1（首页层）：Screen 1 Home——"下一步行动"卡片
L2（详情层）：Screen 2 Plan + Screen 6 InTrip——方案查看和出行跟踪
L3（探索层）：Screen 3 Compare + Screen 4 Client + Screen 5 Culture + Screen 7 Taste + Screen 8 Strategy
```

**关键原则：90%的用户时间应该停留在L0和L1。** 如果用户频繁进入L3，说明L1的信息不够用。

---

## 三、逐页交互深度分析

### Screen 1：首页 Home

#### 3.1.1 用户到达此页时的状态

| 场景 | 用户心理状态 | 设计应对 |
|------|------------|---------|
| 有即将出行的计划 | "我需要确认一下安排" | 显示"即将出行"卡片，置顶 |
| 正在出行中 | "现在到哪了？下一步做什么？" | 显示"进行中"卡片，置顶（比"即将出行"更高优先级） |
| 没有出行计划 | "下次出差前我需要记住什么？" | 显示语音入口 + 空状态引导 |
| 收到主动推送打开App | "有什么重要的事？" | 直接展示推送对应的内容 |

#### 3.1.2 交互层次分析

```
第1层（零交互）：用户看到卡片上的关键信息
  - 目的地、时间、客户名、价格
  - 用户不需要任何操作，0.5秒就能获取核心信息

第2层（一键交互）：用户点击卡片
  - 跳转到方案详情（Screen 2）
  - 1次点击完成导航

第3层（语音交互）：用户点击麦克风
  - 长按说话 → 松开 → 看到识别结果 → 自动跳转方案页
  - 0次点击，1次长按+说话

第4层（零交互·主动）：用户收到推送通知
  - 锁定屏幕/手表显示"合肥出差方案已就绪"
  - 向左滑动 → 直接进入方案页
  - 0次App内操作
```

#### 3.1.3 关键交互决策

**为什么首页没有搜索框？**

> 搜索框是"用户知道自己想要什么"的交互范式。但商务出行场景中，用户往往只知道"要去见谁"，不知道"该选什么航班、什么酒店、什么餐厅"。搜索框在这种情况下是无效的——用户无法搜索自己不知道的东西。
>
> 因此，随行用"语音/文字描述意图"替代"搜索"——这是两种完全不同的交互范式。

**为什么"进行中"卡片比"即将出行"更靠上？**

> 这是时间敏感度排序。"进行中"卡片的信息时效性以分钟计（"接机师傅到了吗？""航班延误了吗？"），而"即将出行"的信息时效性以天计。在视觉层级上，时间越紧迫的信息越靠上。

**为什么卡片上要有"得体度"评分？**

> 这是给用户一个"无需思考的信任锚点"。用户不需要理解为什么推荐这家餐厅，只要看到"得体度 9.2"，就知道"大概率不会出错"。这是将复杂的策展逻辑压缩成一个用户可感知的简单指标。

---

### Screen 2：方案结果 Plan

#### 3.2.1 核心交互模式：时间线浏览

时间线不是装饰——它是商务出行的**自然心智模型**。用户思考商务出行时，脑子里就是一条时间线："早上出发→中午到→吃饭→拜访→回去"。

```
交互优势：
  1. 符合用户心智模型（不需要学习）
  2. 一步了然（不需要翻多页）
  3. 当前步骤自动高亮（用户始终知道"现在该看哪"）
  4. 每个节点可展开/收起（渐进式披露）
```

#### 3.2.2 交互层次分析

```
第1层（浏览）：滚动时间线
  - 用户上下滑动，了解全天安排
  - 每个节点默认显示核心信息（时间+事件+价格）
  - 认知负担：低（像看日历一样自然）

第2层（展开）：点击节点展开详情
  - 显示策展人手记 + 美学评分 + 注意事项
  - 认知负担：中（主动获取深层信息）

第3层（决策）：底部CTA
  - "就这个方案" = 确认（主要操作）
  - "看看备选" = 探索（次要操作）
  - 认知负担：低（二选一）

第4层（关联跳转）：点击客户名或城市标签
  - 跳转到客户档案（Screen 4）或商务须知（Screen 5）
  - 认知负担：中（离开当前页面）
```

#### 3.2.3 关键交互决策

**为什么默认只显示1个方案，而不是3个？**

> 这是"策展"与"搜索"的本质区别。搜索给你10个结果让你选，策展替你选了最好的1个。Hick's Law：选择数量越多，决策时间越长。1个方案 = 用户只需判断"行不行"，3个方案 = 用户需要判断"哪个更好"——后者认知负担是前者的3倍以上。
>
> 但也不隐藏备选——"看看备选"按钮给了用户主动探索的自由。这是"默认简洁 + 按需深入"的渐进式披露。

**为什么策展人手记要放在折叠区域里，而不是默认展开？**

> 手记是"信任背书"，不是"决策必需"。第一次用随行的用户需要看手记来建立信任（"原来AI是这样想的"），但用了10次的用户已经信任随行了，手记就变成冗余信息。
>
> 解决方案：
> - 新用户：手记默认展开（建立信任）
> - 老用户：手记默认收起（减少干扰）
> - 用户行为触发：如果用户停留在一个节点超过5秒，自动展开该节点的手记（检测到用户可能在犹豫）

---

### Screen 3：方案对比 Compare

#### 3.3.1 核心交互模式：空间并排比较

对比3个方案，不是简单的信息罗列——需要让用户在**视觉上直观感受到差异**。

```
设计策略：
  1. 横向等宽排列（而非纵向列表）→ 用户可以左右扫视直接对比
  2. 进度条可视化评分（而非纯数字）→ 视觉感知比数字快3倍
  3. 推荐方案有金色边框装饰 → 预锚定用户的注意力
  4. 每个方案底部有独立CTA → 不需要先选中再确认（减少1步操作）
```

#### 3.3.2 交互层次分析

```
第1层（扫视）：用眼对比
  - 用户2-3秒扫过3列 → 进度条长短一目了然 → 推荐方案的金色边框引导注意力
  - 0次操作，纯视觉判断

第2层（深入）：横向滑动
  - 屏幕宽度可能不够并排3列 → 允许左右滑动查看更多
  - 用户用拇指左右划动

第3层（选择）：点击方案底部的CTA
  - 点击后直接返回Screen 2，方案已替换
  - 1次操作完成选择

第4层（退出）：点击关闭或返回
  - 不选任何方案，保持原方案不变
```

#### 3.3.3 关键交互决策

**为什么推荐方案不给金色边框而是给金色左边框？**

> 金色边框 = "这是最好的"（强烈暗示）
> 金色左边框 = "这是我们的推荐"（温和建议）
>
> 商务人士反感被强迫做选择。强引导（金色边框）会让他们觉得"AI在替我做决定"，弱引导（左边框）让他们觉得"AI在给我建议，但最终我来定"。
>
> 这是"建议的修辞学"——同样的推荐，不同的表达方式，用户接受度差距可达40%。

**为什么3列并排而不是3行纵向？**

> 纵向排列 = 用户需要上下滚动来对比，每次只能看到1个方案的完整信息，对比需要记忆（"方案A的人均是280，方案B是多少来着？"）。
>
> 并排排列 = 用户的眼睛可以水平扫视，3个方案的同维度信息（价格/评分/特点）在同一水平线上，不需要记忆。
>
> 这是格式塔的"接近性原则"——要对比的信息放在一起，用户不需要记忆。

---

### Screen 4：客户档案 ClientProfile

#### 3.4.1 核心交互模式：渐进式记忆录入

客户档案页的核心交互不是"查看"——而是**"录入"和"更新"**。

```
录入策略（首次添加客户）：
  30秒极简录入 → 立刻体现价值 → 鼓励持续维护

  录入字段设计：
  - 必填（3项）：姓名、公司、职位 → 5秒完成
  - 选填（口味/忌口）：标签式点选 → 10秒完成
  - 可延后（个人背景/备注）：文本输入 → 15秒完成
  - 自动抓取（拜访历史）：每次拜访后自动生成，无需手动录入

  价值即现：
  录入完成后立即显示："下次见王总时我会提醒你这些信息"
  → 用户立刻理解"为什么值得花30秒录入"
```

#### 3.4.2 交互层次分析

```
第1层（查看）：浏览档案
  - 头像 + 基本信息 + 口味标签 → 0.5秒获取全貌
  - 拜访历史时间线 → 向下滚动查看

第2层（编辑）：修改信息
  - 点击任一字段 → 进入编辑模式
  - 标签：点击添加/删除
  - 文字：点击弹出键盘
  - 拜访记录：快速满意度打分（1-5星）

第3层（行动）：基于档案生成方案
  - "📋 生成本次接待方案" 按钮
  - 跳转Screen 2，客户信息自动填入
  - 1次点击启动

第4层（智能）：AI接待建议区域
  - 自动分析档案 → 生成个性化建议
  - "可以问问儿子留学近况（破冰话题）"
  - 无须操作，纯被动获益
```

#### 3.4.3 关键交互决策

**为什么口味用"喜欢/可以/避免"三级而非"辣度1-5级"？**

> "辣度1-5级"需要用户量化感知（"这个餐厅辣度3级，客户能接受吗？"）——这是认知负担。
>
> "喜欢/可以/避免"三级 = 只需要做定性判断（"这个菜是辣的→在'喜欢'或'可以'范围内→可以点"）。
>
> 这是**信息颗粒度与决策精度匹配**原则：档案信息的颗粒度只需要比推荐算法的精度高一级。太精细的录入 = 浪费用户时间 + 实际用不上。

**为什么拜访历史用时间线而非表格？**

> 表格 = 数据，时间线 = 故事。
>
> 用户回顾拜访历史时，想知道的不是"3月15日人均¥350"，而是"上次聊了什么、氛围怎么样、有什么要注意"。时间线天然适合承载叙事先于数据的场景。

---

### Screen 5：商务须知 CultureGuide

#### 3.5.1 核心交互模式：手风琴式知识浏览

```
手风琴（Accordion）模式的特点：
  ✅ 适合：信息分类清晰（餐饮/酒桌/礼品/礼仪/社交）、每类独立完整
  ✅ 适合：用户通常只关注部分类别
  ❌ 不适合：需要横向对比不同类别（但商务习俗不需要横比）

交互细节：
  - 默认展开第一个分类（"餐饮"——使用频率最高）
  - 其他分类收起但显示第一行预览（让用户知道里面有什么）
  - 展开/收起带300ms过渡动画（避免突兀的内容跳动）
  - 已读过的分类自动标记为"已读"（降低色温）
```

#### 3.5.2 交互层次分析

```
第1层（浏览）：扫视手风琴头部
  - 看到5个分类标题 + 第一行预览
  - 3秒判断"我需要读哪个"
  
第2层（深入）：点击展开感兴趣的类别
  - 展开后阅读完整内容
  - 每条信息用·分点呈现（不是大段文字）
  
第3层（关联）：关联推荐
  - 底部"合肥商务接待推荐"——从知识到行动的桥梁
  - "这个餐厅就在刚才提到的商务须知类别里"
```

#### 3.5.3 关键交互决策

**为什么手风琴比Tab切换好？**

```
Tab模式：
  [🍽️餐饮] [🍷酒桌] [🎁礼品] [🏢拜访] [💡社交]
  ┌──────────────────────────────┐
  │  只有1个分类的内容可见          │
  └──────────────────────────────┘
  问题：用户不知道其他Tab里有什么，需要逐个点击探索。

手风琴模式：
  🍽️ 餐饮（展开）→ 全部可见
  🍷 酒桌（收起）→ 看到第一行预览"安徽商务酒桌比较务实..."
  🎁 礼品（收起）→ 看到预览"徽茶是安全选择..."
  
  优势：用户一眼看到所有分类的概要，再决定深入哪个。
```

---

### Screen 6：出行中时间线 InTrip

#### 3.6.1 核心交互模式：被动型主动守护

这是整个App中交互最"特别"的页面——用户**不需要操作，只需要感知**。

```
设计核心：关键时刻才出声

时刻1（静默期-不打扰）：
  航班正常飞行中 → 显示"准点" → 无推送 → 用户可以开会/休息
  
时刻2（提醒期-温和提示）：
  距离降落30分钟 → 手表轻震 → "接机师傅李师傅已到达"
  
时刻3（决策期-需要行动）：
  航班延误 → 推送+"建议改签G8，15:30到达" → 用户需要决策
  
时刻4（价值期-关系浮现）：
  进入"拜访"步骤 → 浮现"上次话题"卡片 → 用户被动获益
```

#### 3.6.2 交互层次分析

```
第1层（被动感知）：看时间线
  - 用户不操作，只查看当前状态
  - 当前步骤高亮 + 后续步骤灰色

第2层（主动操作）：改签/加行程/求助
  - 底部4个按钮 + 手表端操作
  - 每个操作不超过3步完成

第3层（惊喜层）：关系记忆浮现
  - 自动弹出提醒卡片
  - 金色光晕动画吸引注意
  - 600ms淡入，不打断用户

第4层（无声层）：通知静默处理
  - "已通知王总您准时到达"——不需用户操作的自动完成事项
  - 绿勾标记 + 自动归档
```

#### 3.6.3 关键交互决策

**为什么关系记忆卡片用"浮现"而非"弹窗"？**

> 弹窗 = 打断用户当前操作流程 → "你必须现在处理我"
> 浮现 = 在当前页面中自然出现 → "你有空时看看我"
>
> 商务场景中，用户可能在和客户通电话。弹窗突然出现不仅打断通话，还会让用户烦躁。浮现动画是"低调的惊喜感"——用户挂完电话看到金色卡片，是正面体验。

**为什么"通知客户方"是自动的而不是让用户确认？**

> "是否通知王总您将准时到达？"——这是一个没有信息增量的确认。用户99.9%会点"是"。每增加一个无信息增量的确认步骤，用户的耐心就消耗一点。
>
> 设计原则：**用户意图100%确定的操作→自动执行；意图不明确的操作→请求确认。** "通知客户准时到达"属于前者。

---

### Screen 7：品味画像 TasteProfile

#### 3.7.1 核心交互模式：数据故事化

品味画像不是Dashboard——它是**用户的"商务人格"可视化**。

```
数据故事化 ≠ 数据可视化

数据可视化：柱状图、折线图、饼图——冷冰冰的效率工具
数据故事化：品味画像——"你在商务社交中是一个什么样的人"

交互策略：
  1. 先给"我是谁"（品味画像摘要）→ 情感共鸣
  2. 再给"我怎么变"（口味演化时间线）→ 成长叙事
  3. 最后给"我和别人"（行业圈层对比）→ 社交参照
```

#### 3.7.2 关键交互决策

**为什么品味画像用雷达图而不是其他图表？**

> 雷达图（5维：品质/效率/社交/本地化/关系维护）天然传达"这是一个profile/画像"的感觉，而柱状图传达"这是数据分析"。
>
> 商务人士更愿意接受前者——"这是我的商务品味"，而不是"这是我被分析的结果"。

---

### Screen 8：接待策略 Strategy

#### 3.8.1 核心交互模式：参数化AI生成

```
交互流程：
  1. 选择/确认参数（客户/目的/城市/预算）→ 4个下拉框或预填
  2. 点击"生成接待策略" → AI处理（1-2秒）
  3. 展开4个策略卡片（餐厅/话题/礼品/注意事项）
  4. 每张卡片可单独展开/编辑
  5. 底部"基于此策略生成完整行程"
```

#### 3.8.2 关键交互决策

**为什么策略卡片允许手动编辑？**

> 纯AI生成 = 用户是"接受者" → 没有参与感
> AI生成+可编辑 = 用户是"合作者" → 有ownership
>
> 心理学上的"IKEA效应"：人们对自己参与创造的东西评价更高。允许用户微调AI策略 → 用户更满意 → 更信任AI。

---

## 四、10种交互模式库

这些模式在整个App中反复出现，形成一致的交互语言：

### 模式1：卡片-展开（Card-Expand）

```
触发：点击卡片
动画：卡片高度从80px展开到240px（300ms ease-out）
用途：Screen 2时间线节点、Screen 5手风琴、Screen 8策略卡片

实现要点：
  - 展开内容在卡片内部，不遮挡其他元素
  - 展开后自动滚动到可见区域
  - 同一时间只有一个卡片展开（手风琴规则）
```

### 模式2：语音-意图（Voice-Intent）

```
触发：长按麦克风或说"随行随行"
反馈：
  0-500ms：麦克风图标开始脉冲动画
  500ms-说话结束：波形条随音量变化
  说话结束-1.5s：显示识别文字（打字机效果逐字出现）
  1.5s-3s：文字变为"正在为你策划..."
  3s后：自动跳转到方案页

实现要点：
  - 识别过程中用户随时可点击取消
  - 如果识别结果置信度<80%，弹出确认框让用户修正
  - 支持中英文混合（"下周二去见科大讯飞的张总"）
```

### 模式3：时间线-当前标记（Timeline-ActiveMarker）

```
用途：Screen 2和Screen 6中标记当前时间点

视觉：
  ● = 10px圆点，--color-accent，有脉冲动画
  ┃ = 2px竖线，--color-text-tertiary
  ○ = 10px圆点，灰色，无动画

行为：
  - 当前步骤自动滚动到屏幕中央
  - 如果当前步骤改变（如时间推进），圆点平滑移动到下一步
  - 手势拖动可以查看前后步骤
```

### 模式4：美学评分条（AestheticsBar）

```
用途：Screen 3中可视化4个维度的评分

视觉：
  ████████░░  9.2
  ├─ 填充色为 --color-accent
  ├─ 轨道色 rgba(255,255,255,0.08)
  ├─ 高度 6px
  └─ 圆角 3px

动画：
  - 页面加载时，进度条从左到右填充（600ms, ease-out）
  - 4条进度条依次填充，每条间隔100ms（stagger animation）
```

### 模式5：关系记忆浮现（MemoryRecall）

```
用途：Screen 6中自动浮现客户相关信息

触发：用户滚动到"拜访"步骤附近时自动触发

动画：
  0ms：卡片 opacity:0, translateY(12px)
  600ms：卡片 opacity:1, translateY(0)，左边金色边框闪烁一次
  整体：cubic-bezier(0.16,1,0.3,1) 缓动

情感设计：
  - 金色边框 = 温暖的提醒（不是警告的红/黄）
  - 从下浮现 = "你差点错过了这个有用信息"
  - 不只提醒"上次话题"，还建议"附近有徽茶专卖"——从记忆到行动的闭环
```

### 模式6：确认-安心（Confirm-PeaceOfMind）

```
用途：预订确认、方案确认等

步骤：
  1. 用户点击确认按钮
  2. 按钮变为"正在处理..."（loading文字+禁点击）
  3. 1-2秒后变为绿色"✓ 已确认" + 一次轻振动
  4. Toast提示"已全部安排妥当"
  5. 自动返回首页

为什么不用弹窗"预订成功"？
  弹窗 = "这个操作结束了，现在你要点击关闭"
  按钮变绿+toast+自动返回 = "这个操作结束了，我带你回到该去的地方"
  后者减少1次无意义点击。
```

### 模式7：标签-选择（Tag-Select）

```
用途：客户口味偏好、地域标签、行业标签等

视觉：
  - 未选中：灰色背景 + 灰色文字
  - 已选中：颜色背景（绿/黄/红）+ 白色文字
  - 过渡：150ms background-color变化

行为：
  - 点击切换选中/未选中
  - 支持多选（大多数标签场景）
  - 长按标签可以删除（编辑模式）
```

### 模式8：手风琴（Accordion）

```
用途：Screen 5商务须知

规则：
  - 同一时间最多1个展开
  - 点击已展开的头部 = 收起
  - 点击收起的头部 = 先收起当前展开的，再展开被点击的

动画：
  - 展开：max-height从0到内容高度（300ms ease-out）
  - 收起：max-height从内容高度到0（250ms ease-in）
  - 头部箭头：旋转180度（200ms）
```

### 模式9：底部动作面板（BottomSheet）

```
用途：预订确认、客户快速录入、方案调整

视觉：
  - 从底部滑入的圆角白色面板
  - 半透明黑色遮罩（opacity 0.4）
  - 顶部小横条（drag handle，暗示可下拉关闭）

行为：
  - 点击遮罩关闭
  - 下拉drag handle关闭
  - 面板内可以有滚动内容
```

### 模式10：骨架屏（Skeleton）

```
用途：列表加载、方案生成中

视觉：
  - 灰色卡片形状占位
  - 表面有从左到右的波纹高光动画（shimmer）

行为：
  - 数据加载完成后骨架屏淡出，真实内容淡入（300ms crossfade）
  - 如果加载超过3秒，显示"正在为你策划..."文字
  - 如果加载失败，骨架屏替换为"加载失败，点击重试"
```

---

## 五、微交互规范：节奏决定品质感

### 5.1 时间节奏表

| 交互类型 | 持续时间 | 缓动函数 | 说明 |
|---------|---------|---------|------|
| 页面Push | 300ms | ease-out | 从右滑入，减速停止 |
| 页面Pop | 250ms | ease-in | 向右滑出，加速消失 |
| 按钮点击缩放 | 150ms | ease-out | 0.96→1.0 |
| 卡片hover | 200ms | ease-out | 上浮2px |
| 列表加载骨架屏 | 200ms shimmer周期 | linear | 无限循环直到加载完成 |
| 成功反馈checkmark | 400ms | spring | 弹性放大再缩小 |
| Toast出现 | 300ms | ease-out | 从上方滑入 |
| Toast消失 | 200ms | ease-in | 向上滑出 |
| 手风琴展开 | 300ms | ease-out | |
| 手风琴收起 | 250ms | ease-in | |
| 关系记忆浮现 | 600ms | cubic-bezier(0.16,1,0.3,1) | 慢进慢出 |
| 进度条填充 | 600ms | ease-out | 带stagger（每个延迟100ms） |
| 脉冲动画（当前步骤） | 2s循环 | ease-in-out | 缩放1.0→1.15→1.0 |
| 波形动画（语音） | 1.2s循环 | ease-in-out | 高度随机变化 |

### 5.2 触觉反馈矩阵

| 场景 | 反馈类型 | 强度 |
|------|---------|------|
| 预订成功 | 单次轻振 | Light |
| 航班延误警报 | 连续3次 | Heavy |
| 按钮点击 | 极轻振 | 仅iOS系统默认 |
| 语音识别开始 | 单次轻振 | Light |
| Toast出现 | 无振动 | — |

### 5.3 声音反馈

```
语音识别开始："滴"（短促，500Hz正弦波，100ms）
语音识别结束："滴滴"（两声，比开始音高一个八度）
预订成功："叮"（柔和钟声，800Hz，300ms）
⚠️ 商务场景关键原则：所有声音反馈必须低调、短促，不引起尴尬
```

---

## 六、认知负荷管理：为什么这样设计

### 6.1 认知负荷三分类

| 类型 | 定义 | 随行商旅的减负策略 |
|------|------|-----------------|
| **内在认知负荷** | 任务本身的复杂度 | 将"选航班+选酒店+选餐厅+选路线"从4步合并为1步"确认方案" |
| **外在认知负荷** | 界面导致的额外负担 | 消除搜索框、消除列表筛选、消除手动比价——这些都是外在负荷 |
| **关联认知负荷** | 理解和学习新信息的负担 | 策展人手记 = "为什么选这个"——帮助用户理解系统逻辑，加速学习 |

### 6.2 认知负荷预算

用户打开App时的"认知预算"是有限的。每一步操作都在消耗预算：

```
传统商旅App：
  打开App → 搜索框 → 输入目的地 → 选日期 → 搜航班 → 翻3页选航班 
  → 搜酒店 → 翻5页选酒店 → 搜餐厅 → 翻7页选餐厅 → 对比 → 预订
  消耗：10+步，认知预算耗尽 → 用户疲劳 → 放弃或低质量决策

随行商旅：
  打开App → 看到方案 → 确认
  消耗：2步 → 认知预算富余 → 用户轻松 → 高质量决策 + 回来再用
```

### 6.3 信息密度控制

```
L1 首页：信息密度 1/5（只有1-2张卡片）
L2 方案页：信息密度 3/5（1条时间线，多个节点）
L3 对比页：信息密度 4/5（3列并排，需要对比）
L4 策略页：信息密度 3/5（4张策略卡片，可逐张看）

原则：信息密度永远不能超过用户的当前认知预算。
```

---

## 七、交互思考框架：如何系统做交互设计

### 7.1 从"用户任务"出发，而非"功能列表"

```
❌ 错误做法：
  "我们要做语音输入功能" → 设计语音输入界面 → 开发

✅ 正确做法：
  "用户的真实任务是'安排好下周的客户拜访'"
  → 分解任务：确定时间、订交通、选餐厅、记住客户偏好
  → 设计交互：语音说一句话 → AI生成完整方案 → 用户确认
  → 语音输入只是这个交互链的第一步
```

### 7.2 五步交互设计法

```
Step 1：描述用户的"当前时刻"
  问：用户打开App前，他在做什么？想什么？担心什么？
  例：张总刚收到日历提醒"明天见王总"，他担心：吃什么？怎么去？上次聊了什么？

Step 2：定义"理想结果"
  问：用户完成操作后，他应该感到什么？
  例：感到"安排妥当了，明天可以安心见客户"

Step 3：设计"最少步骤路径"
  问：达到理想结果，最少需要几步？
  例：打开App→看到方案→确认 = 3步

Step 4：在关键步骤注入"惊喜"
  问：在哪一步可以超出用户预期？
  例：在方案中自动提醒"上次王总提到儿子在留学"——用户没想到AI记得

Step 5：用反馈闭环检验每一步
  问：每一步用户是否得到了清晰的反馈？
  例：确认→按钮变绿+振动+Toast→用户明确知道"预订成功了"
```

### 7.3 交互设计自查清单

```
□ 用户需要学习才能使用吗？（如果yes，简化）
□ 主要操作能在拇指热区完成吗？（如果no，调整布局）
□ 每一步操作都有即时反馈吗？（如果no，补充反馈）
□ 有"一键回到安全状态"吗？（返回按钮/取消操作）
□ 用户犯错后能撤销吗？（如果no，增加确认步骤）
□ 重要信息在0.5秒内能被扫视到吗？（如果no，优化视觉层级）
□ 老人/新手能用吗？（如果no，增加引导或降低复杂度）
□ 没网络时能用吗？（至少静态内容可浏览）
□ 屏幕很小（SE）和很大（Pro Max）都能用吗？
□ 暗色模式下所有元素清晰可见吗？
```

---

## 附录：关键参考来源

- Nielsen Norman Group — 10 Usability Heuristics
- Hick's Law — Decision time vs. number of choices
- Fitts's Law — Target acquisition time
- Apple Human Interface Guidelines — iOS patterns
- Material Design 3 — Interaction patterns
- Cognitive Load Theory (Sweller, 1988)
- Progressive Disclosure (Nielsen)
- Don't Make Me Think (Steve Krug)
- Microinteractions (Dan Saffer)
