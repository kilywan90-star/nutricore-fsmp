[project]
name = "aimedia-platform"
version = "0.1.0"
description = "AI融媒体平台 - 医疗行业版"
requires-python = ">=3.12"

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
